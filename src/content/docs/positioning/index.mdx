---
title: Positioning
sidebar_label: Positioning
sidebar_position: 3
description: "Placing and stacking elements: centering techniques, the position property, stacking context and z-index, the containing block that traps fixed elements, and anchor positioning."
---

Placing an element is easy; keeping it predictable is not. These pages cover centering, the `position` property, stacking context and `z-index`, the containing block that makes `position: fixed` behave like `absolute`, and anchor positioning.

<CategoryNav category="positioning" />
