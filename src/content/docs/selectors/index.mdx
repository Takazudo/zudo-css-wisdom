---
title: Selectors
sidebar_label: Selectors
sidebar_position: 15
description: Modern selectors like :has(), :is(), and :where() for expressive, JavaScript-free styling.
---

Modern selectors move logic that used to require JavaScript into the stylesheet. These pages cover `:has()`, `:is()` / `:where()`, and styling children from a parent's state.

<CategoryNav category="selectors" />
